export const metadata = {
  title: {
    template: "%s | Donor",
    default: "Donor",
  },
  description: "Eliminating Food Waste",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
